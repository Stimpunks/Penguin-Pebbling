# The game lived at Autistic Realms first, and inside one long page here for a
# day before it was split. Both sets of old addresses land somewhere sensible.
/penguin-pebbling-a-game-of-creating-belonging-building-connection-and-understanding-autistic-identity  /  301

# Single-page-era anchors. These were real URLs people could have copied.
#
# Only the two that no longer name a page need a rule. /print, /how-to-play,
# /locutions and /about are served directly by Netlify's Pretty URLs, which also
# rewrites every `href="x.html"` in the markup to `/x` on the way out — see
# DECISIONS.md. Rules for those would be dead weight at best.
/downloads              /print.html        301
/game                   /                  301
